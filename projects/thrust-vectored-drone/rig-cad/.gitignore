# Derived render output — regenerate with render_anim.py (needs anim/geom.json).
# 120 PNGs, ~9 MB, rewritten in full on every render.
anim/frames/

# FreeCAD backup files
*.FCStd1

__pycache__/
*.pyc
