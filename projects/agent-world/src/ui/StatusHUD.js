/**
 * Status HUD — Phase 5 stub.
 * Will show minimap, all-agents-at-a-glance, and notification toasts.
 * Currently, basic HUD rendering is done inline in UIScene.js.
 */
export class StatusHUD {
  constructor() {
    this.element = document.getElementById('status-hud');
  }

  // Phase 5: minimap, toast notifications, detailed agent cards
}
